#!/bin/bash

env -i osascript -e 'launch app id "com.microsoft.VSCode"'
while [[ "$(osascript -e 'running of app id "com.microsoft.VSCode"')" != true ]]; do
	sleep 0.1
done
exec env -i /usr/local/bin/code "$@"
